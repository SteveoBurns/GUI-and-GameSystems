using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DroppedItem : MonoBehaviour
{
    // This gets added to dropped items, so then they can be picked up again and added back into the inventory.
    public Item item;
}
